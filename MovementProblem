(define (problem simpleMovement)
  (:domain submarine)

  (:objects
      bob
      atlantis
  )

  (:init
      (subLoc bob)
      (subDestination atlantis)
  )

  (:goal
      (and
          (at atlantis)
      )
  )
)
